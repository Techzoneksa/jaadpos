DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
AUTH_SECRET="replace-with-a-long-random-secret"
NEXT_PUBLIC_APP_URL="https://your-domain.com"

# Seed-only values. Keep real passwords outside Git.
SEED_ADMIN_PASSWORD="replace-before-seeding"
SEED_OWNER_PASSWORD="replace-before-seeding"
SEED_CASHIER_PASSWORD="replace-before-seeding"
SEED_ACCOUNTANT_PASSWORD="replace-before-seeding"
